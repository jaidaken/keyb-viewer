//! Reads keymap + physical layout from a ZMK keyboard over the Studio RPC and
//! emits layout.json for the keyb-viewer overlay.
#![warn(clippy::pedantic)]

fn main() -> anyhow::Result<()> {
    Ok(())
}
